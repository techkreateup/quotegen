"use client";

import DemoApp from "@/components/landing/DemoApp";

/* Full-screen mirror demo of the SaaS — public, no backend, sample data. */
export default function DemoPage() {
  return <DemoApp full />;
}
